# PayRelay — Payment Gateway Retry & Reconciliation Orchestrator

Two-service microservices system handling unreliable external payment gateway calls with retry logic and idempotent event processing.

## Results

(bullets added here as each is actually measured — real numbers only)

## Tech Stack

Java 17, Spring Boot, Apache Kafka, PostgreSQL, Redis, Docker Compose
